// import { createSlice, PayloadAction } from "@reduxjs/toolkit";

// interface BreadcrumbState {
//   prevPaths: string[];
// }

// export const initialState: BreadcrumbState = {
//   prevPaths: ["/"],
// };

// export const breadcrumbSlice = createSlice({
//   name: "breadcrumb",
//   initialState,
//   reducers: {
//     addPath: (state, action: PayloadAction<string>) => {
//       state.prevPaths.push(action.payload);
//     },
//     popPath: (state) => {
//       state.prevPaths.pop();
//     },
//   },
// });

// export const { addPath, popPath } = breadcrumbSlice.actions;
